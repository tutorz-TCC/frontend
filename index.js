/**
 * @format
 */

import { AppRegistry } from "react-native";
import { name as appName } from "./app.json";
import Cadastro from "./screens/Cadastro";

AppRegistry.registerComponent(appName, () => Cadastro);
